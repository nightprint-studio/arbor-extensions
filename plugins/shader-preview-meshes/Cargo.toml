[package]
name        = "shader-preview-meshes"
version     = "1.0.0"
edition.workspace = true
license.workspace = true
description = "The four meshes a shader preview starts with, as a wasm component."

[lib]
name       = "shader_preview_meshes"
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = { workspace = true }
serde_json  = { workspace = true }
